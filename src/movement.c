#include "types.h"
int generateMoves(Position position,  Move* moveList){
    //Check for hashed moves

    switch(position.flag & WHITE_TURN){
        case 0:
            break;
        case 1:
            break;
        default:
            break;
    }

    //Check for capturing moves
    //Check for killer moves
    //Check rest of moves
}